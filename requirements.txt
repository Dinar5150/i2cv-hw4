numpy>=1.24
scipy>=1.10
plyfile>=0.9
imageio[ffmpeg]>=2.31
# Used by gsplat runtime helpers.
packaging>=23.0
# Optional GPU renderer dependencies (install separately):
#   pip install torch --extra-index-url https://download.pytorch.org/whl/cu121
#   pip install git+https://github.com/nerfstudio-project/gsplat.git
